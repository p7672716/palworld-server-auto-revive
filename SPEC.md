# Palworld Server Auto Revive — 仕様書

## 1. 目的

サーバー権限で通知される3イベントの直後に、対象プレイヤーの瀕死パルだけをゲーム本来の蘇生処理へ渡す。クライアントMODは使用しない。

対象サーバーの前提:

- Palworld Dedicated Server、ネイティブLinux版
- Steam AppID: 2394010
- 初期確認時のbuildid: 24445026
- Debian 13
- custom Lua-free UE4SS Linux native layer
- native C++ MOD

Palworld本体更新で反射関数名や内部処理が変わる可能性があるため、ゲーム更新後は必ずフックのロードログと受け入れ試験を再実施する。

## 2. 用語

### 2.1 瀕死

次の両方を満たすパル:

1. ゲームの `CharacterParameterComponent:IsDying()` が真。
2. `IndividualParameter:GetHP()` の固定小数点値が0。

`IsDead()` の恒久的な死亡状態は対象にしない。HP 0だけで、ゲームの瀕死状態に入っていないパルも対象にしない。

### 2.2 蘇生

対象パルの `CharacterParameterComponent:ReviveFromDying()` を呼ぶ。MODはHP値、復帰HP率、最大HP、空腹、SAN、負傷、病気を直接変更しない。

`FullRecoveryHP()`、HP直接設定、セーブファイル直接書換え、Palbox全走査は実装しない。

## 3. イベント仕様

| イベント | 検出フック | 対象 |
| --- | --- | --- |
| パルボックスへ入れる確定後 | native bridgeの `SetOtomoSlot` pre/post | 移動確定したその1体だけ |
| リスポーン完了直後 | `PalPlayerCharacter:CallRespawnDelegate` | そのプレイヤーの手持ちパーティ全体 |
| 自ギルド拠点へ外から進入直後 | `PalBuilderComponent:OnEnterBaseCamp` | そのプレイヤーの手持ちパーティ全体 |

### 3.1 パルボックス移動

native bridgeは`SetOtomoSlot`のpre段で、指定スロットにいた個体を1体だけ退避し、post段でその同一ポインタだけを検査する。ボックス全走査はしない。`SetOtomoSlot`がパーティ並べ替えにも使われる本体更新では、イベント引数を再確認してから有効化する。

これにより、手持ちパーティから別の場所へ移動する更新や、パーティ内の通常入替えを蘇生イベントと誤認しない。Palboxの全スロット、全ページ、他の個体は走査しない。

### 3.2 リスポーン

`CallRespawnDelegate` の対象コンテキストをプレイヤーとして、対応する `PalOtomoHolderComponentBase` の全ハンドルを列挙する。列挙範囲は手持ちパーティのみで、Palboxやギルド拠点の作業パルは含めない。

### 3.3 拠点進入

`OnEnterBaseCamp` に渡された拠点モデルのGuild IDと、プレイヤーの `PlayerState.GuildBelongTo` のGuild IDを比較する。一致した場合だけ手持ちパーティ全体を処理する。

このフックはゲーム側の境界進入通知を利用するため、継続滞在、別ギルド拠点、通知を伴わないファストトラベル／再配置は処理しない。ゲーム側が同じ `OnEnterBaseCamp` を発火する場合は仕様上処理対象となる。

## 4. 非対象

以下は対象外:

- クライアント側だけで発生する通知
- プレイヤーの死亡そのもの、死亡前、リスポーン完了前
- Palbox内の既存個体の定期走査
- 手持ち以外のパル
- 別ギルドの拠点
- 拠点内に居続けている状態
- ゲーム側の拠点進入通知を伴わない再配置
- 空腹、負傷、病気、SAN低下、状態異常、装備、経験値の変更
- 全回復や最大HPへの設定変更

## 5. 冪等性と安全性

- 蘇生前に毎回 `IsDying` とHP 0を確認する。
- `IsDead` または無効なオブジェクトはスキップする。
- 同じイベントが複数回通知されても、1回目の蘇生後は瀕死判定を満たさないため再処理しない。
- すべてのゲームオブジェクト操作は可能な限りゲームスレッドへスケジュールする。
- ログにはイベント名、処理数、スキップ数だけを出し、プレイヤーUIDやセーブ内容を出さない。

## 6. 受け入れ基準

### A. 基盤

- セーブバックアップの作成日時、パス、SHA256が記録されている。
- UE4SS Linuxアーカイブの公開SHA256と実体のSHA256が一致する。
- `libUE4SS.so` がPalServer実行ファイルと同じLinuxバイナリディレクトリにある。
- systemdサービスに `LD_PRELOAD` が永続設定されている。
- サービス再起動後にUE4SSと本MODのロードログが出る。
- Palworld本体だけで起動できるロールバック手順が残っている。

### B. イベント

各テストは、対象パル以外に瀕死パルを1体以上用意して、誤対象がないことも確認する。

1. 瀕死パル1体を手持ちからPalboxへ移す。
   - 移動した1体だけが即時に蘇生する。
   - Palbox内の別の瀕死パルは変化しない。
2. 瀕死パルを手持ちに含めた状態でプレイヤーを死亡させ、リスポーンする。
   - リスポーン完了後、手持ちの瀕死パル全体が蘇生する。
   - Palbox内や拠点作業中のパルは対象にならない。
3. 瀕死パルを手持ちに含め、同じギルドの拠点の外から境界内へ入る。
   - 境界進入通知の直後、手持ちの瀕死パル全体が蘇生する。
4. 別ギルド拠点、拠点内の継続滞在、通知のないファストトラベル再配置。
   - MODによる追加蘇生が発生しない。
5. 瀕死ではないパルに空腹、負傷、病気、SAN低下を付ける。
   - MODだけではそれらを治療しない。
6. 蘇生後のHPを確認する。
   - MODが全回復値を書き込まず、ゲーム本来の `ReviveFromDying` の復帰値になる。

## 7. ロールバック

1. サービス停止。
2. systemdのUE4SS drop-inを退避または削除し、daemon-reload。
3. `Mods/mods.txt` の本MOD行を削除または0へ変更。
4. `Mods/PalworldServerAutoRevive` を別名へ退避。
5. `libUE4SS.so`、`UE4SS-settings.ini`、UE4SS関連設定を退避。
6. 必要なら、MOD導入前バックアップからセーブとLinuxServer設定を復元。
7. PalServerを基盤なしで起動し、ログインとセーブを確認する。

## 8. 既知の制約

- UE4SS Linuxはコミュニティのネイティブ移植版で、Palworld公式のWindows専用サーバーMOD案内の範囲外。
- 対象Linux実行ファイルはstrip済み。UE4SSが自動解決できない関数は、`UE4SS_Addresses.ini` または本体更新対応が必要になる。
- PalworldのUFunction名やパーティ／拠点イベント実装は本体更新で変わり得る。
- 実環境でプレイヤー操作を伴うA〜Bの受け入れ試験が完了するまでは、動作確認済みとは扱わない。
